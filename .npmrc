# Use hoisted node_modules so Next.js can resolve workspace packages.
node-linker=hoisted

# Don't fail on peer-dependency warnings — shadcn/Radix ecosystem is noisy.
strict-peer-dependencies=false

# Auto-install peers for clarity.
auto-install-peers=true

# Save exact versions for cross-package consistency in this monorepo.
save-exact=false
