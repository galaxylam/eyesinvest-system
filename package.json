{
  "name": "eyesinvest-system",
  "version": "0.1.0",
  "private": true,
  "description": "EyesInvest — professional investment-analysis platform (US + HK equities)",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "e2e": "turbo run e2e",
    "clean": "turbo run clean && rm -rf node_modules .turbo",
    "worker:sync": "cd workers/yfinance && uv run python -m eyesinvest_worker all",
    "worker:sync:us": "cd workers/yfinance && uv run python -m eyesinvest_worker all-us",
    "worker:sync:hk": "cd workers/yfinance && uv run python -m eyesinvest_worker all-hk"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "prettier": "^3.4.2"
  }
}
