/**
 * Supabase generated types placeholder.
 *
 * Once `local/supabase` is running and migrations are applied, regenerate
 * with:
 *
 *   supabase gen types typescript --local > packages/types/src/database.ts
 *
 * Until then, callers should rely on the DTOs in ./stock and ./market
 * rather than reaching into the database types directly.
 */

export type DatabasePlaceholder = never;
